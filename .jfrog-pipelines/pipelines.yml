resources:
  - name: exclude
    type: GitRepo
    configuration:
      gitProvider: vijay
      path: vijayreddy1991/run       
      files:
        exclude: test.yaml
        
  - name: include
    type: GitRepo
    configuration:
      gitProvider: vijay
      path: vijayreddy1991/run       
      files:
        include: test.yaml
pipelines:
  - name: test_gcp_nodepool
    configuration:  
      runtime:
        type: host
    steps:
      - name: exclude_step
        type: Bash
        configuration:  
          runtime:
            type: host        
          inputResources:
            - name: exclude
        execution:
          onExecute: 
             - echo "testing"
             - docker images
             # - docker --version
             # - node --version
             # - npm --version
             # - npm list -g kind-of
             # - npm list -g json5
             # - npm list -g elliptic
      - name: include_step
        type: Bash
        configuration:
          # nodePool: win_2019
          # runtime:
          #   type: host
          inputResources:
            - name: include
        execution:
          onExecute: 
             - echo "testing"
#              - cat logs.txt
#              - cat startlogs.txt             
