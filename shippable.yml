resources:
      
  - name: vijayRepo
    type: GitRepo
    configuration:
      gitProvider: vijay
      path: vijayreddy1991/run

pipelines:
  - name: vijay_pipeline
    steps:

      - name: vijay_step
        type: Bash
        configuration: 
          nodePool: vijay          
          inputResources:
            - name: vijayRepo

        execution:
          onExecute:
            - echo "testvariable"

